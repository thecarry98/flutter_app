import 'package:flutter/material.dart';

class SecondBranch extends StatelessWidget {
  const SecondBranch({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('second branch'),
    ));
  }
}
